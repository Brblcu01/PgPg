spring:
  application:
    name: proconsul-sso

server:
  port: ${SERVER_PORT:8081}

management:
  server:
    port: 8081
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health
